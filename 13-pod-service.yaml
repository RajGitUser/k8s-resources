apiVersion: v1
kind: Pod
metadata: 
  name: service-pod
  ## below lables are used to register the pod with the service
  labels:
    project: roboshop
    component: frontend
    environment: dev
spec:
  containers:
  - name: nginx
    image: nginx